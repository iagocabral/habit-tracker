class HabitCategory {
  final String name;
  final String iconPath; // Caminho para o ícone da categoria
  final String description;

  HabitCategory({
    required this.name,
    required this.iconPath,
    required this.description,
  });
}